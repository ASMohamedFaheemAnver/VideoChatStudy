const config = {
  appID: "Number",
  token: "tToken or From server, I am using tToken for dev purpose",
  userId: "Any",
  userName: "Any",
  server: "Look cloud console",
};

export default config;
